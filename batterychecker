while true
do
        bat=$(acpi -i | grep harging | sed 's/[^,]*, // ; s/%.*//')

        if [ $bat -lt 20 ]
        then
                notify-send -u critical "$bat"
        fi
        echo $bat
        sleep 300
done
