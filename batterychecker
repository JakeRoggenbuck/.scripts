while true
do
        bat=$(acpi -i | grep Dis | sed 's/[^,]*, // ; s/%.*//')

        if [ $bat -lt 20 ]
        then
                notify-send -u critical "Battery $bat%"
        fi
        echo $bat
        sleep 300
done
